package inheritanceStrategy.mappedSuperClass;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

public class MSClient {

    public static void main(String[] args) {

        /*EntityManagerFactory emf = Persistence.createEntityManagerFactory("BugTracker");
        EntityManager em = emf.createEntityManager();


        // persist librarian
        Librarian l1 = new Librarian(101, "John Doe", "password", 1000);
        Student s1 = new Student(202, "Andrew Nguyen", "123", "S21");

        em.getTransaction().begin();
        em.persist(l1);
        em.persist(s1);
        em.getTransaction().commit();*/

    }
}
