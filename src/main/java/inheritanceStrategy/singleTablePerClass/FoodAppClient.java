package inheritanceStrategy.singleTablePerClass;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

public class FoodAppClient {

    public static void main(String[] args) {
        /*EntityManagerFactory emf = Persistence.createEntityManagerFactory("BugTracker");
        EntityManager em = emf.createEntityManager();

        Customer c1 = new Customer(200, "Andrew Blake", "pizza, pasta, cookies");
        Owner o1 = new Owner(300, "Mary Tan", "Pizzzzza");
        em.getTransaction().begin();
        em.persist(c1);
        em.persist(o1);
        em.getTransaction().commit();*/
    }
}
